flask>=3.0
requests>=2.31
gunicorn>=21.2

# Hero-image generation/sourcing scripts (scripts/generate_hero_images.py,
# scripts/fetch_hero_images.py). Not needed to run the web app itself.
Pillow>=10.0
huggingface_hub>=0.25
